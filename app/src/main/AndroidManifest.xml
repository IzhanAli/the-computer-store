<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.izhandroid.bookdemo1">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".Login"
            android:theme="@style/Login">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OrderHistory"
            android:label="Order History"
            android:parentActivityName=".MainActivity"
            android:theme="@style/ActTheme" />
        <activity
            android:name=".MobCheckout"
            android:label="Mobile Store"
            android:parentActivityName=".MobStore"
            android:theme="@style/ActTheme" />
        <activity
            android:name=".MobStore"
            android:label="Mobile Store"
            android:parentActivityName=".MainActivity"
            android:theme="@style/ActTheme" />
        <activity
            android:name=".CompCheckout"
            android:label="Computer Store"
            android:parentActivityName=".CompStore"
            android:theme="@style/ActTheme" />
        <activity
            android:name=".CompStore"
            android:label="Computer Store"
            android:parentActivityName=".MainActivity"
            android:theme="@style/ActTheme" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>